// Stub replacement for imagemin-avif
// Returns identity function to avoid breaking builds
module.exports = (options = {}) => {
  return (buffer) => {
    console.warn('imagemin-avif: Using stub, no AVIF optimization performed');
    return Promise.resolve(buffer);
  };
};
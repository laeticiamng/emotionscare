// @ts-nocheck
/**
 * setupTestUser — DISABLED
 *
 * This module previously seeded a test user into the database at app startup.
 * It has been removed to prevent accidental execution in production environments.
 *
 * For local development testing, use Supabase seed scripts or the SQL editor instead.
 */

// No-op — kept as placeholder so existing imports (if any) don't break.
export {};

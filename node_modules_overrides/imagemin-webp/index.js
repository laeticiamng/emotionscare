// Stub replacement for imagemin-webp
// Returns identity function to avoid breaking builds
module.exports = (options = {}) => {
  return (buffer) => {
    console.warn('imagemin-webp: Using stub, no WebP optimization performed');
    return Promise.resolve(buffer);
  };
};
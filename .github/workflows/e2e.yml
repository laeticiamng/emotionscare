name: E2E

on:
  pull_request:

jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - run: echo "E2E tests require Supabase secrets and a preview environment. Configure E2E_ENABLED=true in repository variables to enable."

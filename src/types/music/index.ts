
// Correctly re-export music types
export * from '../music';

// If there's a default export in music.ts, we need to handle it separately
// import musicDefault from '../music';
// export { musicDefault as default };
